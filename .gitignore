__pycache__/
*.pyc
processamento.log
erros.log
CODIFICADOR.zip
config.json
build/
dist/
# Specs gerados na hora pelo PyInstaller ficam de fora, mas a receita de
# build do projeto é versionada de propósito — sem ela, só quem tem o
# ambiente montado consegue gerar o .exe.
*.spec
!codificador.spec
.pytest_cache/
tests/__pycache__/
# Cópias de segurança do cadastro (feitas antes de importações em massa) —
# a planilha em si é versionada, o backup é só rede local.
cadastro_condominios_BACKUP_*.xlsx
# Ambiente virtual local (as dependências vêm de requirements.txt).
.venv/

# Worktrees de execução de planos (Claude Code) — nunca versionados
.claude/worktrees/
.superpowers/
